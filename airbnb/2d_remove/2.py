class TwoDIterator:
    def __init__(self, clist):
        self.clist=clist
        self.rowIdx=0
        self.colIdx=0

    def next(self):
        if self.rowIdx>=len(clist):
            return False
        if self.colIdx>=len(self.rowIdx):

    def hasNext(self):
        while self.rowIdx<len()
